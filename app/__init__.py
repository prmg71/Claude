"""Analisador de Contratos — pacote da aplicação.

Organização:
  app.config        -> configuração (env, caminhos)
  app.core          -> motor de análise (extração, LLM, análise, relatório)
  app.knowledge     -> Base de Conhecimento (RAG) por tipo de contrato
  app.cli           -> interface de linha de comando
"""
